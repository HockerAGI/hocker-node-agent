# ⚠️ Deprecated: usa .env.example (mismas variables).
# Este archivo se mantiene SOLO para compatibilidad.

# Supabase (SERVICE ROLE)
SUPABASE_URL="https://xxxx.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="SUPABASE_SERVICE_ROLE_KEY_AQUI"

# Identidad del agente
PROJECT_ID="global"
NODE_ID="hocker-node-1"

# Debe ser EXACTAMENTE el mismo secreto que firma comandos desde hocker.one / nova.agi
COMMAND_HMAC_SECRET="pon_una_clave_larga_aqui"

# Seguridad sandbox (solo lectura)
SANDBOX_ROOT="./sandbox"
READ_DIR_LIMIT="200"
FILE_HEAD_BYTES="65536"

# Loop
POLL_MS="2000"